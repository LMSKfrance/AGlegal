[build]
  command = "npx drizzle-kit push && (node --import=tsx scripts/seed-admin.ts || echo 'Seed skipped, continuing build...') && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
